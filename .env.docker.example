APP_NAME=Laravel
APP_ENV=production
APP_KEY=base64:CHANGE_THIS_IN_PRODUCTION
APP_DEBUG=false
APP_URL=http://localhost

SANCTUM_STATEFUL_DOMAINS=localhost:5173,localhost:3000
FRONTEND_URL=http://localhost:5173

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

LOG_CHANNEL=stack
LOG_STACK=single
LOG_LEVEL=info

# PostgreSQL (Docker)
DB_CONNECTION=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=nurturing
DB_USERNAME=segal
DB_PASSWORD=password

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_SECURE_COOKIES=false

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local

# Redis Queue & Cache (Docker)
QUEUE_CONNECTION=redis
CACHE_STORE=redis

# Redis Configuration
REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log

# Access Token: expira rápido (15-60 minutos)
SANCTUM_ACCESS_TOKEN_EXPIRATION=15

# Refresh Token: expira lento (7-30 días)
REFRESH_TOKEN_EXPIRATION=7

# AthenaCampaign API Configuration
ATHENACAMPAIGN_BASE_URL=https://apimail.athenacampaign.com
ATHENACAMPAIGN_API_KEY=YOUR_API_KEY_HERE
